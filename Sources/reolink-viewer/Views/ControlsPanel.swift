import SwiftUI

/// Placeholder. Milestone 4 puts the siren, floodlight, PTZ pad, presets, and
/// manual record controls here, each gated on `Capabilities`.
struct ControlsPanel: View {
    var body: some View {
        EmptyView()
    }
}
